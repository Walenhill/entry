# Система записи на услуги (MVP)

Простой прототип системы бронирования для проверки идеи. Без лишних сложностей — только базовый функционал.

## Что умеет

**Для клиентов:**
- Посмотреть свободные слоты
- Забронировать слот (имя + телефон)

**Для администратора:**
- Войти по паролю (сессионная авторизация)
- Создать слот вручную
- Сгенерировать слоты по шаблону (например, каждый час с 9 до 18)
- Видеть все брони и статистику
- Отменять/удалять слоты

## Технологии

- **Бэкенд:** PHP 8.2 (чистый, без фреймворков) + MySQL
- **Фронтенд:** Vue 3 + Vite
- **Инфраструктура:** Docker

## Как запустить

### Вариант 1: Docker (рекомендуется)

```bash
# 1. Скопируйте конфиги
cp backend/.env.example backend/.env
cp fronted-vue/.env.example fronted-vue/.env

# 2. Отредактируйте пароли в backend/.env

# 3. Запустите
docker-compose up -d
```

Приложение доступно:
- Фронтенд: http://localhost:5173
- Бэкенд API: http://localhost:8080

### Вариант 2: Без Docker

**Бэкенд:**
```bash
cd backend
# Создайте .env файл с настройками БД
php -S localhost:8080
```

**Фронтенд:**
```bash
cd fronted-vue
npm install
npm run dev
```

## Структура проекта

```
/workspace
├── backend/           # PHP API
│   ├── main.php      # Точка входа
│   ├── api.php       # Обработчики запросов
│   └── includes/     # Логика
│       ├── auth.php        # Авторизация и безопасность
│       ├── helpers.php     # Вспомогательные функции
│       └── slots_crud.php  # CRUD слотов
├── fronted-vue/      # Vue 3 приложение
└── docker-compose.yml
```

## API (кратко)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/auth/login` | Вход админа (создает сессию) |
| POST | `/auth/logout` | Выход админа |
| GET | `/slots` | Список слотов |
| POST | `/slots` | Создать слот (админ) |
| POST | `/slots/generate` | Генерация по шаблону (админ) |
| POST | `/slots/{id}/book` | Забронировать |
| POST | `/slots/{id}/cancel` | Отменить бронь (админ) |
| DELETE | `/slots/{id}` | Удалить слот (админ) |
| GET | `/stats` | Статистика (админ) |

Подробности — в разделе "API Documentation" ниже.

## Troubleshooting

**Ошибка подключения к БД:**
- Проверьте `backend/.env` (пароль, имя базы)
- Убедитесь, что MySQL запустился: `docker-compose ps`

**CORS ошибки:**
- Убедитесь, что фронтенд стучится на `http://localhost:8080`
- Проверьте `.env` во фронтенде (`VITE_API_BASE_URL`)

**Не работает авторизация:**
- Пароль по умолчанию: `admin123`
- После первого входа пароль хэшируется и сохраняется в БД
- Для смены пароля нужно изменить MASTER_PASSWORD в `.env` и очистить таблицу `settings`

---

## API Documentation

### Authentication

#### POST /auth/login
Вход в систему. Создает защищенную сессию.

**Тело запроса:**
```json
{ "password": "admin123" }
```

**Ответ (успех):**
```json
{ 
  "success": true,
  "message": "Login successful"
}
```
*Примечание: Сессия управляется через куки (HttpOnly, Secure)*

**Ответ (ошибка):**
```json
{ 
  "error": "Invalid password"
}
```
*После 5 неудачных попыток IP блокируется на 15 минут*

#### POST /auth/logout
Выход из системы (только админ)

**Ответ:**
```json
{ 
  "success": true
}
```

### Slots

#### GET /slots
Получить слоты. Клиенты видят только свободные, админы - все.

**Параметры:**
- `date` (optional) - фильтрация по дате (YYYY-MM-DD)
- `role` (optional) - 'admin' для получения всех слотов (требуется авторизация)

**Ответ:**
```json
[
  {
    "id": 1,
    "start_time": "2024-06-01 10:00:00",
    "end_time": "2024-06-01 11:00:00",
    "description": "Утренний слот",
    "status": "available"
  }
]
```

#### POST /slots
Создать новый слот (требуется авторизация)

**Тело запроса:**
```json
{
  "start_time": "2024-06-01 10:00:00",
  "end_time": "2024-06-01 11:00:00",
  "description": "Утренний слот"
}
```

**Валидация:**
- Формат даты/времени: YYYY-MM-DD HH:MM:SS
- start_time должен быть раньше end_time
- Отсутствие пересечений с существующими слотами

#### POST /slots/generate
Сгенерировать слоты по шаблону (требуется авторизация)

**Тело запроса:**
```json
{
  "date": "2024-06-01",
  "start_hour": 9,
  "end_hour": 18,
  "duration": 60,
  "description": "Консультация"
}
```

#### POST /slots/:id/book
Забронировать слот

**Тело запроса:**
```json
{
  "client_name": "Иван Иванов",
  "client_phone": "+79991234567"
}
```

**Ответ:**
```json
{ 
  "success": true,
  "slot": { ... }
}
```

#### POST /slots/:id/cancel
Отменить бронь (требуется авторизация)

**Ответ:**
```json
{ 
  "success": true,
  "slot": { ... }
}
```

#### DELETE /slots/:id
Удалить слот (требуется авторизация)

**Ответ:**
```json
{ 
  "success": true,
  "message": "Slot deleted successfully"
}
```

#### GET /stats
Получить статистику (требуется авторизация)

**Ответ:**
```json
{
  "success": true,
  "data": {
    "summary": {
      "total": 50,
      "booked": 35,
      "available": 10,
      "cancelled": 5
    },
    "occupancy_rate": 70.0,
    "top_clients": [
      {
        "client_name": "Иван Иванов",
        "client_phone": "+79991234567",
        "visits": 5
      }
    ]
  }
}
```

## Технологии

**Бэкенд:**
- PHP 8.2 (без фреймворков)
- MySQL 8.0
- Apache

**Фронтенд:**
- Vue 3 (Composition API)
- Vite
- Axios

**Инфраструктура:**
- Docker & Docker Compose

## Структура проекта

```
/workspace
├── backend/
│   ├── main.php          # API endpoint
│   ├── init.sql          # Схема БД
│   ├── .env              # Конфигурация
│   └── Dockerfile
├── fronted-vue/
│   ├── src/
│   │   ├── components/
│   │   │   ├── LoginForm.vue
│   │   │   └── SlotsManager.vue
│   │   ├── api/
│   │   │   └── index.js
│   │   └── App.vue
│   ├── .env
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## Переменные окружения

**backend/.env:**
- `DB_HOST` - хост MySQL
- `DB_USERNAME` - пользователь БД
- `DB_PASSWORD` - пароль БД
- `DB_NAME` - имя БД
- `MASTER_PASSWORD` - пароль администратора
- `SECRET_KEY` - секретный ключ для токенов

**fronted-vue/.env:**
- `VITE_API_BASE_URL` - URL бэкенда

## Безопасность

- Пароли хэшируются через Argon2id и хранятся в БД
- Сессионная авторизация с защищенными куки (HttpOnly, SameSite)
- Rate limiting: блокировка IP после 5 неудачных попыток на 15 минут
- Валидация всех входных данных
- Защита от пересечения слотов по времени
- Разделение доступа: клиенты видят только свободные слоты

## Changelog

### v0.4.0 - Современная безопасность
- 🔐 Переход на password_hash() с Argon2id
- 🍪 Сессионная авторизация вместо самописных токенов
- 🚫 Rate limiting для защиты от брутфорса
- 📁 Новый файл includes/auth.php
- 🗑 Удалены устаревшие функции генерации токенов
- 📝 Обновлена документация API

### v0.3.0 - Рефакторинг бэкенда
- 📁 Разделение main.php на логические файлы (api.php, includes/)
- 🔧 Улучшена структура кода и читаемость
- ✅ Вся функциональность сохранена
- 📊 Добавлена статистика (/stats)

### v0.2.0 - Безопасность и валидация
- ✅ Валидация формата даты/времени
- ⛔ Запрет пересечения слотов
- 🔑 SECRET_KEY в конфигурации

### v0.1.0 — MVP
- Базовый CRUD слотов
- Бронирование клиентами
- Админ-панель
- Docker конфигурация
